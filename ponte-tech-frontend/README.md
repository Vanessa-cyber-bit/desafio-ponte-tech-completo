# 🚀 Frontend - Desafio PonteTech

Este é o projeto Frontend (React) do desafio.

## 🚦 Como Rodar

**Importante:** Este projeto **não funciona sozinho**. Ele precisa do Backend (na pasta `ponte-tech-desafio`) rodando na porta `3000`.

1.  Garanta que o Backend (com Docker) esteja rodando.
2.  Abra um terminal nesta pasta.
3.  Instale as dependências:
    ```bash
    npm install
    ```
4.  Inicie o servidor do React:
    ```bash
    npm start
    ```
5.  O app abrirá em `http://localhost:3001`.